// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/iot-synergy/openned8-rpc/ent/industryinfo"
	"github.com/iot-synergy/openned8-rpc/ent/predicate"
)

// IndustryInfoQuery is the builder for querying IndustryInfo entities.
type IndustryInfoQuery struct {
	config
	ctx        *QueryContext
	order      []industryinfo.OrderOption
	inters     []Interceptor
	predicates []predicate.IndustryInfo
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the IndustryInfoQuery builder.
func (iiq *IndustryInfoQuery) Where(ps ...predicate.IndustryInfo) *IndustryInfoQuery {
	iiq.predicates = append(iiq.predicates, ps...)
	return iiq
}

// Limit the number of records to be returned by this query.
func (iiq *IndustryInfoQuery) Limit(limit int) *IndustryInfoQuery {
	iiq.ctx.Limit = &limit
	return iiq
}

// Offset to start from.
func (iiq *IndustryInfoQuery) Offset(offset int) *IndustryInfoQuery {
	iiq.ctx.Offset = &offset
	return iiq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (iiq *IndustryInfoQuery) Unique(unique bool) *IndustryInfoQuery {
	iiq.ctx.Unique = &unique
	return iiq
}

// Order specifies how the records should be ordered.
func (iiq *IndustryInfoQuery) Order(o ...industryinfo.OrderOption) *IndustryInfoQuery {
	iiq.order = append(iiq.order, o...)
	return iiq
}

// First returns the first IndustryInfo entity from the query.
// Returns a *NotFoundError when no IndustryInfo was found.
func (iiq *IndustryInfoQuery) First(ctx context.Context) (*IndustryInfo, error) {
	nodes, err := iiq.Limit(1).All(setContextOp(ctx, iiq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{industryinfo.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (iiq *IndustryInfoQuery) FirstX(ctx context.Context) *IndustryInfo {
	node, err := iiq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first IndustryInfo ID from the query.
// Returns a *NotFoundError when no IndustryInfo ID was found.
func (iiq *IndustryInfoQuery) FirstID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = iiq.Limit(1).IDs(setContextOp(ctx, iiq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{industryinfo.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (iiq *IndustryInfoQuery) FirstIDX(ctx context.Context) uint64 {
	id, err := iiq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single IndustryInfo entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one IndustryInfo entity is found.
// Returns a *NotFoundError when no IndustryInfo entities are found.
func (iiq *IndustryInfoQuery) Only(ctx context.Context) (*IndustryInfo, error) {
	nodes, err := iiq.Limit(2).All(setContextOp(ctx, iiq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{industryinfo.Label}
	default:
		return nil, &NotSingularError{industryinfo.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (iiq *IndustryInfoQuery) OnlyX(ctx context.Context) *IndustryInfo {
	node, err := iiq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only IndustryInfo ID in the query.
// Returns a *NotSingularError when more than one IndustryInfo ID is found.
// Returns a *NotFoundError when no entities are found.
func (iiq *IndustryInfoQuery) OnlyID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = iiq.Limit(2).IDs(setContextOp(ctx, iiq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{industryinfo.Label}
	default:
		err = &NotSingularError{industryinfo.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (iiq *IndustryInfoQuery) OnlyIDX(ctx context.Context) uint64 {
	id, err := iiq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of IndustryInfos.
func (iiq *IndustryInfoQuery) All(ctx context.Context) ([]*IndustryInfo, error) {
	ctx = setContextOp(ctx, iiq.ctx, "All")
	if err := iiq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*IndustryInfo, *IndustryInfoQuery]()
	return withInterceptors[[]*IndustryInfo](ctx, iiq, qr, iiq.inters)
}

// AllX is like All, but panics if an error occurs.
func (iiq *IndustryInfoQuery) AllX(ctx context.Context) []*IndustryInfo {
	nodes, err := iiq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of IndustryInfo IDs.
func (iiq *IndustryInfoQuery) IDs(ctx context.Context) (ids []uint64, err error) {
	if iiq.ctx.Unique == nil && iiq.path != nil {
		iiq.Unique(true)
	}
	ctx = setContextOp(ctx, iiq.ctx, "IDs")
	if err = iiq.Select(industryinfo.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (iiq *IndustryInfoQuery) IDsX(ctx context.Context) []uint64 {
	ids, err := iiq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (iiq *IndustryInfoQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, iiq.ctx, "Count")
	if err := iiq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, iiq, querierCount[*IndustryInfoQuery](), iiq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (iiq *IndustryInfoQuery) CountX(ctx context.Context) int {
	count, err := iiq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (iiq *IndustryInfoQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, iiq.ctx, "Exist")
	switch _, err := iiq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (iiq *IndustryInfoQuery) ExistX(ctx context.Context) bool {
	exist, err := iiq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the IndustryInfoQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (iiq *IndustryInfoQuery) Clone() *IndustryInfoQuery {
	if iiq == nil {
		return nil
	}
	return &IndustryInfoQuery{
		config:     iiq.config,
		ctx:        iiq.ctx.Clone(),
		order:      append([]industryinfo.OrderOption{}, iiq.order...),
		inters:     append([]Interceptor{}, iiq.inters...),
		predicates: append([]predicate.IndustryInfo{}, iiq.predicates...),
		// clone intermediate query.
		sql:  iiq.sql.Clone(),
		path: iiq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.IndustryInfo.Query().
//		GroupBy(industryinfo.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (iiq *IndustryInfoQuery) GroupBy(field string, fields ...string) *IndustryInfoGroupBy {
	iiq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &IndustryInfoGroupBy{build: iiq}
	grbuild.flds = &iiq.ctx.Fields
	grbuild.label = industryinfo.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.IndustryInfo.Query().
//		Select(industryinfo.FieldCreatedAt).
//		Scan(ctx, &v)
func (iiq *IndustryInfoQuery) Select(fields ...string) *IndustryInfoSelect {
	iiq.ctx.Fields = append(iiq.ctx.Fields, fields...)
	sbuild := &IndustryInfoSelect{IndustryInfoQuery: iiq}
	sbuild.label = industryinfo.Label
	sbuild.flds, sbuild.scan = &iiq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a IndustryInfoSelect configured with the given aggregations.
func (iiq *IndustryInfoQuery) Aggregate(fns ...AggregateFunc) *IndustryInfoSelect {
	return iiq.Select().Aggregate(fns...)
}

func (iiq *IndustryInfoQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range iiq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, iiq); err != nil {
				return err
			}
		}
	}
	for _, f := range iiq.ctx.Fields {
		if !industryinfo.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if iiq.path != nil {
		prev, err := iiq.path(ctx)
		if err != nil {
			return err
		}
		iiq.sql = prev
	}
	return nil
}

func (iiq *IndustryInfoQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*IndustryInfo, error) {
	var (
		nodes = []*IndustryInfo{}
		_spec = iiq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*IndustryInfo).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &IndustryInfo{config: iiq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, iiq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (iiq *IndustryInfoQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := iiq.querySpec()
	_spec.Node.Columns = iiq.ctx.Fields
	if len(iiq.ctx.Fields) > 0 {
		_spec.Unique = iiq.ctx.Unique != nil && *iiq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, iiq.driver, _spec)
}

func (iiq *IndustryInfoQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(industryinfo.Table, industryinfo.Columns, sqlgraph.NewFieldSpec(industryinfo.FieldID, field.TypeUint64))
	_spec.From = iiq.sql
	if unique := iiq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if iiq.path != nil {
		_spec.Unique = true
	}
	if fields := iiq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, industryinfo.FieldID)
		for i := range fields {
			if fields[i] != industryinfo.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := iiq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := iiq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := iiq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := iiq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (iiq *IndustryInfoQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(iiq.driver.Dialect())
	t1 := builder.Table(industryinfo.Table)
	columns := iiq.ctx.Fields
	if len(columns) == 0 {
		columns = industryinfo.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if iiq.sql != nil {
		selector = iiq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if iiq.ctx.Unique != nil && *iiq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range iiq.predicates {
		p(selector)
	}
	for _, p := range iiq.order {
		p(selector)
	}
	if offset := iiq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := iiq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// IndustryInfoGroupBy is the group-by builder for IndustryInfo entities.
type IndustryInfoGroupBy struct {
	selector
	build *IndustryInfoQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (iigb *IndustryInfoGroupBy) Aggregate(fns ...AggregateFunc) *IndustryInfoGroupBy {
	iigb.fns = append(iigb.fns, fns...)
	return iigb
}

// Scan applies the selector query and scans the result into the given value.
func (iigb *IndustryInfoGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, iigb.build.ctx, "GroupBy")
	if err := iigb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*IndustryInfoQuery, *IndustryInfoGroupBy](ctx, iigb.build, iigb, iigb.build.inters, v)
}

func (iigb *IndustryInfoGroupBy) sqlScan(ctx context.Context, root *IndustryInfoQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(iigb.fns))
	for _, fn := range iigb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*iigb.flds)+len(iigb.fns))
		for _, f := range *iigb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*iigb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := iigb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// IndustryInfoSelect is the builder for selecting fields of IndustryInfo entities.
type IndustryInfoSelect struct {
	*IndustryInfoQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (iis *IndustryInfoSelect) Aggregate(fns ...AggregateFunc) *IndustryInfoSelect {
	iis.fns = append(iis.fns, fns...)
	return iis
}

// Scan applies the selector query and scans the result into the given value.
func (iis *IndustryInfoSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, iis.ctx, "Select")
	if err := iis.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*IndustryInfoQuery, *IndustryInfoSelect](ctx, iis.IndustryInfoQuery, iis, iis.inters, v)
}

func (iis *IndustryInfoSelect) sqlScan(ctx context.Context, root *IndustryInfoQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(iis.fns))
	for _, fn := range iis.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*iis.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := iis.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
