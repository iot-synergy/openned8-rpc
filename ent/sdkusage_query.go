// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	uuid "github.com/gofrs/uuid/v5"
	"github.com/iot-synergy/openned8-rpc/ent/predicate"
	"github.com/iot-synergy/openned8-rpc/ent/sdkusage"
)

// SdkUsageQuery is the builder for querying SdkUsage entities.
type SdkUsageQuery struct {
	config
	ctx        *QueryContext
	order      []sdkusage.OrderOption
	inters     []Interceptor
	predicates []predicate.SdkUsage
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SdkUsageQuery builder.
func (suq *SdkUsageQuery) Where(ps ...predicate.SdkUsage) *SdkUsageQuery {
	suq.predicates = append(suq.predicates, ps...)
	return suq
}

// Limit the number of records to be returned by this query.
func (suq *SdkUsageQuery) Limit(limit int) *SdkUsageQuery {
	suq.ctx.Limit = &limit
	return suq
}

// Offset to start from.
func (suq *SdkUsageQuery) Offset(offset int) *SdkUsageQuery {
	suq.ctx.Offset = &offset
	return suq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (suq *SdkUsageQuery) Unique(unique bool) *SdkUsageQuery {
	suq.ctx.Unique = &unique
	return suq
}

// Order specifies how the records should be ordered.
func (suq *SdkUsageQuery) Order(o ...sdkusage.OrderOption) *SdkUsageQuery {
	suq.order = append(suq.order, o...)
	return suq
}

// First returns the first SdkUsage entity from the query.
// Returns a *NotFoundError when no SdkUsage was found.
func (suq *SdkUsageQuery) First(ctx context.Context) (*SdkUsage, error) {
	nodes, err := suq.Limit(1).All(setContextOp(ctx, suq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{sdkusage.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (suq *SdkUsageQuery) FirstX(ctx context.Context) *SdkUsage {
	node, err := suq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SdkUsage ID from the query.
// Returns a *NotFoundError when no SdkUsage ID was found.
func (suq *SdkUsageQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = suq.Limit(1).IDs(setContextOp(ctx, suq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{sdkusage.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (suq *SdkUsageQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := suq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SdkUsage entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SdkUsage entity is found.
// Returns a *NotFoundError when no SdkUsage entities are found.
func (suq *SdkUsageQuery) Only(ctx context.Context) (*SdkUsage, error) {
	nodes, err := suq.Limit(2).All(setContextOp(ctx, suq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{sdkusage.Label}
	default:
		return nil, &NotSingularError{sdkusage.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (suq *SdkUsageQuery) OnlyX(ctx context.Context) *SdkUsage {
	node, err := suq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SdkUsage ID in the query.
// Returns a *NotSingularError when more than one SdkUsage ID is found.
// Returns a *NotFoundError when no entities are found.
func (suq *SdkUsageQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = suq.Limit(2).IDs(setContextOp(ctx, suq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{sdkusage.Label}
	default:
		err = &NotSingularError{sdkusage.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (suq *SdkUsageQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := suq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SdkUsages.
func (suq *SdkUsageQuery) All(ctx context.Context) ([]*SdkUsage, error) {
	ctx = setContextOp(ctx, suq.ctx, "All")
	if err := suq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SdkUsage, *SdkUsageQuery]()
	return withInterceptors[[]*SdkUsage](ctx, suq, qr, suq.inters)
}

// AllX is like All, but panics if an error occurs.
func (suq *SdkUsageQuery) AllX(ctx context.Context) []*SdkUsage {
	nodes, err := suq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SdkUsage IDs.
func (suq *SdkUsageQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if suq.ctx.Unique == nil && suq.path != nil {
		suq.Unique(true)
	}
	ctx = setContextOp(ctx, suq.ctx, "IDs")
	if err = suq.Select(sdkusage.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (suq *SdkUsageQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := suq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (suq *SdkUsageQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, suq.ctx, "Count")
	if err := suq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, suq, querierCount[*SdkUsageQuery](), suq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (suq *SdkUsageQuery) CountX(ctx context.Context) int {
	count, err := suq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (suq *SdkUsageQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, suq.ctx, "Exist")
	switch _, err := suq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (suq *SdkUsageQuery) ExistX(ctx context.Context) bool {
	exist, err := suq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SdkUsageQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (suq *SdkUsageQuery) Clone() *SdkUsageQuery {
	if suq == nil {
		return nil
	}
	return &SdkUsageQuery{
		config:     suq.config,
		ctx:        suq.ctx.Clone(),
		order:      append([]sdkusage.OrderOption{}, suq.order...),
		inters:     append([]Interceptor{}, suq.inters...),
		predicates: append([]predicate.SdkUsage{}, suq.predicates...),
		// clone intermediate query.
		sql:  suq.sql.Clone(),
		path: suq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SdkUsage.Query().
//		GroupBy(sdkusage.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (suq *SdkUsageQuery) GroupBy(field string, fields ...string) *SdkUsageGroupBy {
	suq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SdkUsageGroupBy{build: suq}
	grbuild.flds = &suq.ctx.Fields
	grbuild.label = sdkusage.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.SdkUsage.Query().
//		Select(sdkusage.FieldCreatedAt).
//		Scan(ctx, &v)
func (suq *SdkUsageQuery) Select(fields ...string) *SdkUsageSelect {
	suq.ctx.Fields = append(suq.ctx.Fields, fields...)
	sbuild := &SdkUsageSelect{SdkUsageQuery: suq}
	sbuild.label = sdkusage.Label
	sbuild.flds, sbuild.scan = &suq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SdkUsageSelect configured with the given aggregations.
func (suq *SdkUsageQuery) Aggregate(fns ...AggregateFunc) *SdkUsageSelect {
	return suq.Select().Aggregate(fns...)
}

func (suq *SdkUsageQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range suq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, suq); err != nil {
				return err
			}
		}
	}
	for _, f := range suq.ctx.Fields {
		if !sdkusage.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if suq.path != nil {
		prev, err := suq.path(ctx)
		if err != nil {
			return err
		}
		suq.sql = prev
	}
	return nil
}

func (suq *SdkUsageQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SdkUsage, error) {
	var (
		nodes = []*SdkUsage{}
		_spec = suq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SdkUsage).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SdkUsage{config: suq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, suq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (suq *SdkUsageQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := suq.querySpec()
	_spec.Node.Columns = suq.ctx.Fields
	if len(suq.ctx.Fields) > 0 {
		_spec.Unique = suq.ctx.Unique != nil && *suq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, suq.driver, _spec)
}

func (suq *SdkUsageQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(sdkusage.Table, sdkusage.Columns, sqlgraph.NewFieldSpec(sdkusage.FieldID, field.TypeUUID))
	_spec.From = suq.sql
	if unique := suq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if suq.path != nil {
		_spec.Unique = true
	}
	if fields := suq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, sdkusage.FieldID)
		for i := range fields {
			if fields[i] != sdkusage.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := suq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := suq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := suq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := suq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (suq *SdkUsageQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(suq.driver.Dialect())
	t1 := builder.Table(sdkusage.Table)
	columns := suq.ctx.Fields
	if len(columns) == 0 {
		columns = sdkusage.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if suq.sql != nil {
		selector = suq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if suq.ctx.Unique != nil && *suq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range suq.predicates {
		p(selector)
	}
	for _, p := range suq.order {
		p(selector)
	}
	if offset := suq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := suq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// SdkUsageGroupBy is the group-by builder for SdkUsage entities.
type SdkUsageGroupBy struct {
	selector
	build *SdkUsageQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (sugb *SdkUsageGroupBy) Aggregate(fns ...AggregateFunc) *SdkUsageGroupBy {
	sugb.fns = append(sugb.fns, fns...)
	return sugb
}

// Scan applies the selector query and scans the result into the given value.
func (sugb *SdkUsageGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sugb.build.ctx, "GroupBy")
	if err := sugb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SdkUsageQuery, *SdkUsageGroupBy](ctx, sugb.build, sugb, sugb.build.inters, v)
}

func (sugb *SdkUsageGroupBy) sqlScan(ctx context.Context, root *SdkUsageQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(sugb.fns))
	for _, fn := range sugb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*sugb.flds)+len(sugb.fns))
		for _, f := range *sugb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*sugb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sugb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SdkUsageSelect is the builder for selecting fields of SdkUsage entities.
type SdkUsageSelect struct {
	*SdkUsageQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (sus *SdkUsageSelect) Aggregate(fns ...AggregateFunc) *SdkUsageSelect {
	sus.fns = append(sus.fns, fns...)
	return sus
}

// Scan applies the selector query and scans the result into the given value.
func (sus *SdkUsageSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sus.ctx, "Select")
	if err := sus.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SdkUsageQuery, *SdkUsageSelect](ctx, sus.SdkUsageQuery, sus, sus.inters, v)
}

func (sus *SdkUsageSelect) sqlScan(ctx context.Context, root *SdkUsageQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(sus.fns))
	for _, fn := range sus.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*sus.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sus.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
